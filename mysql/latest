FROM kusprayitna/base:alpine


RUN	apk add mysql mysql-client rsyslog --no-cache \ 	
	&& rm /var/cache/apk/* -f

VOLUME 	/var/lib/mysql
	
ENV 	ENV_LOGSERVER -

COPY    mysql/settings /etc
COPY	mysql/starts /etc

EXPOSE	3306

ENTRYPOINT ["sh","/etc/start.sh"]
